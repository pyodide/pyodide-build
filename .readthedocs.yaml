version: 2

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

formats: all

python:
  install:
    - requirements: docs/requirements-doc.txt
    - method: pip
      path: .

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
